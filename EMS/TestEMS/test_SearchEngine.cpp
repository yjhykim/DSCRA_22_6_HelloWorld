#include "pch.h"
#include "../EMS/SearchEngine.h"
#include <iostream>
using namespace std;

TEST(TEST_SEARCHENGINE, 범위밖사번찾기) {
    SearchEngine se;

    //EXPECT_EQ(0, size(se.Search(db, 65000000)));
    //EXPECT_EQ(0, size(se.Search(db, 22000000)));
}